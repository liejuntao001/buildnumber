version: '3'

services:
  gogs:
    image: baibai/buildnumber
    container_name: buildnumber
    ports:
     - "127.0.0.1:8080:8080"
    volumes:
     - /data/buildnumber:/data
    environment:
     - STORAGE_DIR=/data
    restart: always
